# This file generates the GitHub Actions matrix.
# For simple board + shield combinations, add them to the top level board and
# shield arrays, for more control, add individual board + shield combinations
# to the `include` property. You can also use the `cmake-args` property to
# pass flags to the build command, `snippet` to add a Zephyr snippet, and
# `artifact-name` to assign a name to distinguish build outputs from each other:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#   - board: nice_nano_v2
#     shield: corne_left
#     snippet: studio-rpc-usb-uart
#     cmake-args: -DCONFIG_ZMK_STUDIO=y
#     artifact-name: corne_left_with_studio
#
---
include:
  - board: xiao_ble/nrf52840/zmk
    shield: totem_left
    snippet: studio-rpc-usb-uart
    cmake-args: -DCONFIG_ZMK_STUDIO=y
    artifact-name: totem_left
  - board: xiao_ble/nrf52840/zmk
    shield: totem_right
    artifact-name: totem_right
  # Diagnostic build: same right half, plus a USB serial console.
  # Plug this half into the PC and read the split-link logs.
  - board: xiao_ble/nrf52840/zmk
    shield: totem_right
    snippet: zmk-usb-logging
    cmake-args: -DCONFIG_LOG_PROCESS_THREAD_STARTUP_DELAY_MS=20000 -DCONFIG_LOG_BUFFER_SIZE=32768
    artifact-name: totem_right_LOGGING
  - board: xiao_ble/nrf52840/zmk
    shield: settings_reset
    artifact-name: settings_reset
  # Diagnostic build of the CENTRAL. No Studio -- the USB CDC endpoint
  # can only carry one of Studio or the log console, and right now the
  # log is what we need. Temporary; reflash totem_left.uf2 afterwards.
  - board: xiao_ble/nrf52840/zmk
    shield: totem_left
    snippet: zmk-usb-logging
    cmake-args: -DCONFIG_LOG_PROCESS_THREAD_STARTUP_DELAY_MS=20000 -DCONFIG_LOG_BUFFER_SIZE=32768
    artifact-name: totem_left_LOGGING
